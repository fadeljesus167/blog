<%= form_with model: @user, url: :new_user do |form| %>
    <% if @user.errors.any? %>
        <% @user.errors.full_messages.each do |error| %>
            <div class="alert alert-danger"><%= error %></div>
        <% end %>
    <% end %>

    <div class="mb-3">
        <%= form.label :username %>
        <%= form.text_field :username %>
    </div>

    <div class="mb-3">
        <%= form.label :password %>
        <%= form.password_field :password %>
    </div>

    <div>
        <%= form.submit t('.submit') %>
    </div>
<% end %>